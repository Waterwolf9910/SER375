{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Solution",
            "command": "godot-mono --build-solutions --quit --headless ${workspaceFolder}/project.godot",
            "type": "shell",
            // "args": [],
            // "problemMatcher": [
            //     "$tsc"
            // ],
            "presentation": {
                "reveal": "always"
            },
            "group": "build"
        }
    ]
}
