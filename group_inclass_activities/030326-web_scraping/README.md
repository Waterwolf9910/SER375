# 030326-web_scraping

## To Run

- Make sure nodejs is installed
- Enable yarn if not already enabled `corepack enable yarn` (sudo needed if on linux)
- run `yarn` or `yarn install` to add all depenedencies
- run `yarn build` to "build" the project
- run `yarn start` to execute the application
